PythonAnywhere has version 0.21.3 of sklearn.

Check version inside Python:
import sklearn
sklearn.__version__

To install in PA, need to detail the pip version:
pip3.8 install --user --upgrade --force-reinstall scikit-learn  

Install a particular version:
pip install scikit-learn==0.21.3

Install all the requirements:
pip install -r requirements.txt